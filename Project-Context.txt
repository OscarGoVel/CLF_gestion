# CLF Sistema de Gestión — Requerimientos Técnicos y Lógica de Base de Datos

> **RFC de la empresa:** CLF240418U94  
> **Versión del documento:** 1.0  
> **Fecha:** 2026-05-05  
> **Propósito:** Referencia técnica para desarrollo, onboarding de IA y auditorías. Este archivo describe el negocio, la arquitectura, el esquema de datos y las reglas de negocio que gobiernan el sistema.

---

## 1. Contexto del Negocio

CLF es una empresa **distribuidora/comercializadora B2B** (business-to-business) en México. Compra productos a proveedores y los revende a clientes corporativos, emitiendo y recibiendo facturas electrónicas CFDI según la normativa del SAT.

### Flujo operativo principal

```
Cliente solicita → Cotización → (cliente autoriza) → Compra a proveedor
→ Entrega al cliente → Facturación → Cobro
```

El sistema gestiona este ciclo completo: catálogos, cotizaciones, compras, inventario, facturas CFDI y análisis financiero.

---

## 2. Arquitectura del Sistema

### Tecnología

| Capa | Tecnología |
|---|---|
| Backend web | Python 3.13 + FastAPI |
| Plantillas HTML | Jinja2 + HTMX (interactividad sin JS pesado) |
| CSS | Tailwind CSS |
| Base de datos | PostgreSQL (migrado desde SQLite) |
| Autenticación | JWT (HS256, TTL 8 horas) + PBKDF2-HMAC-SHA256 |
| App de escritorio (legacy) | Python + Tkinter |
| Generación de PDF | Librería PDF personalizada |
| Servidor web | Uvicorn / Nginx (reverse proxy) |

### Bases de datos

El sistema usa **dos bases de datos separadas en PostgreSQL**:

- `clf_usuarios` — usuarios globales y preferencias (compartida entre todas las empresas)
- `clf_empresa` (o el nombre configurado por empresa) — datos operativos de cada empresa

Esto permite arquitectura **multi-empresa**: una instalación puede gestionar más de una razón social.

### Módulos / Routers

| Router | Prefijo URL | Función |
|---|---|---|
| `catalogos` | `/catalogos` | Clientes, proveedores, productos |
| `cotizaciones` | `/cotizaciones` | Ciclo de ventas completo |
| `compras` | `/compras` | Registro de compras a proveedores |
| `facturas` | `/facturas` | Importación y vinculación de CFDI |
| `stock` | `/stock` | Inventario y movimientos |
| `preinventario` | `/preinventario` | Conteo físico de inventario |
| `estudio_mercado` | `/estudio-mercado` | Estudio de precios de mercado |
| `estado_cuenta` | `/estado-cuenta` | Estado de cuenta por cliente |
| `analisis` | `/analisis` | Análisis de costos y métricas |
| `admin` | `/admin` | Usuarios, permisos, ubicaciones |
| `herramientas` | `/herramientas` | CFDI, vinculación, utilidades |
| `cfdi` | `/cfdi` | Importación masiva de XMLs |

---

## 3. Esquema de Base de Datos

### 3.1 Base: `clf_usuarios` (global)

```sql
usuarios (
    id             SERIAL PRIMARY KEY,
    username       TEXT UNIQUE NOT NULL,
    nombre         TEXT NOT NULL,
    password_hash  TEXT NOT NULL,   -- PBKDF2-HMAC-SHA256, 200,000 iteraciones
    salt           TEXT,            -- salt aleatorio por usuario (hex 32 bytes)
    rol            TEXT NOT NULL DEFAULT 'Operador',
    activo         INTEGER DEFAULT 1,
    fecha_creacion TIMESTAMPTZ DEFAULT NOW(),
    ultimo_acceso  TIMESTAMPTZ
)

preferencias_usuario (
    usuario_id INTEGER NOT NULL,
    clave      TEXT NOT NULL,
    valor      TEXT NOT NULL DEFAULT '',
    PRIMARY KEY (usuario_id, clave)
)
```

### 3.2 Base: `clf_empresa` (por empresa)

#### Catálogos

```sql
clientes (
    id               SERIAL PRIMARY KEY,
    nombre_comercial TEXT NOT NULL,
    razon_social     TEXT,
    tipo             TEXT NOT NULL,       -- 'Empresa', 'Gobierno', 'Persona física', etc.
    rfc              TEXT,
    direccion        TEXT,
    contacto         TEXT,
    telefono         TEXT,
    email            TEXT,
    regimen_fiscal   TEXT,                -- para timbrado CFDI
    uso_cfdi         TEXT,
    cp_fiscal        TEXT,
    corporativo_id   INTEGER,             -- FK a corporativos (grupo empresarial)
    activo           BOOLEAN DEFAULT TRUE,
    fecha_registro   TIMESTAMPTZ DEFAULT NOW()
)

corporativos (
    id     SERIAL PRIMARY KEY,
    nombre TEXT NOT NULL UNIQUE           -- agrupa clientes del mismo grupo empresarial
)

proveedores (
    id             SERIAL PRIMARY KEY,
    nombre         TEXT NOT NULL,
    razon_social   TEXT,
    rfc            TEXT,
    direccion      TEXT,
    contacto       TEXT,
    telefono       TEXT,
    email          TEXT,
    notas          TEXT,
    regimen_fiscal TEXT,
    cp_fiscal      TEXT,
    uso_cfdi       TEXT,
    fecha_registro TIMESTAMPTZ DEFAULT NOW()
)

categorias (
    id     SERIAL PRIMARY KEY,
    nombre TEXT NOT NULL UNIQUE
)

subcategorias (
    id           SERIAL PRIMARY KEY,
    categoria_id INTEGER REFERENCES categorias(id),
    nombre       TEXT NOT NULL
)

metodos_pago (
    id          SERIAL PRIMARY KEY,
    nombre      TEXT UNIQUE NOT NULL,    -- Efectivo, Transferencia, Tarjeta, etc.
    descripcion TEXT,
    activo      INTEGER DEFAULT 1
)
```

#### Productos e inventario

```sql
productos (
    id                SERIAL PRIMARY KEY,
    codigo            TEXT UNIQUE NOT NULL,
    nombre            TEXT NOT NULL,
    descripcion       TEXT,
    categoria_id      INTEGER REFERENCES categorias(id),
    subcategoria_id   INTEGER REFERENCES subcategorias(id),
    unidad_medida     TEXT,
    precio_base       NUMERIC(14,4) NOT NULL,   -- precio sin IVA
    aplica_iva        INTEGER DEFAULT 1,
    precio_venta      NUMERIC(14,4),            -- precio con margen aplicado
    stock_actual      NUMERIC(14,4) DEFAULT 0,
    stock_minimo      NUMERIC(14,4) DEFAULT 0,
    clave_sat         TEXT,                     -- clave producto/servicio SAT (CFDI)
    clave_unidad_sat  TEXT,                     -- clave unidad SAT
    precio_base_fecha TEXT,                     -- fecha de la última actualización de precio_base
    costo_promedio    NUMERIC(14,4),            -- calculado a partir de compras (costo promedio ponderado)
    fecha_registro    TIMESTAMPTZ DEFAULT NOW()
)

producto_proveedor (
    producto_id    INTEGER NOT NULL REFERENCES productos(id),
    proveedor_id   INTEGER NOT NULL REFERENCES proveedores(id),
    es_principal   INTEGER DEFAULT 0,           -- 1 = proveedor preferido
    notas          TEXT,
    UNIQUE(producto_id, proveedor_id)
)

producto_claves_sat (
    producto_id    INTEGER NOT NULL REFERENCES productos(id),
    clave_sat      TEXT NOT NULL,
    fuente         TEXT DEFAULT 'xml_import',   -- origen: manual o importado de CFDI
    UNIQUE(producto_id, clave_sat)
)

producto_precio_historial (
    id          SERIAL PRIMARY KEY,
    producto_id INTEGER NOT NULL REFERENCES productos(id),
    proveedor_id INTEGER REFERENCES proveedores(id),
    precio      NUMERIC(14,4) NOT NULL,
    fecha       TEXT NOT NULL,
    motivo      TEXT,
    fuente      TEXT DEFAULT 'manual'
)

movimientos_stock (
    id            SERIAL PRIMARY KEY,
    producto_id   INTEGER NOT NULL REFERENCES productos(id),
    tipo          TEXT NOT NULL,            -- 'entrada' | 'salida' | 'ajuste'
    motivo        TEXT,                     -- Merma/Daño, Uso interno, etc.
    cantidad      NUMERIC(14,4) NOT NULL,
    stock_antes   NUMERIC(14,4) NOT NULL,
    stock_despues NUMERIC(14,4) NOT NULL,
    referencia    TEXT,                     -- folio de compra, cotización, etc.
    notas         TEXT,
    usuario       TEXT,
    fecha         TIMESTAMPTZ DEFAULT NOW()
)
```

#### Cotizaciones (flujo de ventas)

```sql
cotizaciones (
    id                SERIAL PRIMARY KEY,
    folio             TEXT UNIQUE NOT NULL,     -- formato: COT-YYYY-NNNN
    fecha             DATE NOT NULL,
    cliente_id        INTEGER NOT NULL REFERENCES clientes(id),
    subtotal          NUMERIC(14,4) DEFAULT 0,
    iva               NUMERIC(14,4) DEFAULT 0,
    total             NUMERIC(14,4) DEFAULT 0,
    aplica_iva        INTEGER DEFAULT 0,
    notas             TEXT,
    observaciones     TEXT,
    estado            TEXT DEFAULT 'Pendiente', -- ver máquina de estados
    orden_compra      TEXT,
    fecha_orden_compra DATE,
    fecha_entrega     DATE,
    fecha_factura     DATE,
    fecha_pago        DATE,
    monto_entregado   NUMERIC(14,4) DEFAULT 0,
    monto_facturado   NUMERIC(14,4) DEFAULT 0,
    monto_pagado      NUMERIC(14,4) DEFAULT 0,
    numero_factura    TEXT,
    entrega_parcial   INTEGER DEFAULT 0,        -- 1 = tiene entregas parciales
    oc_documento      TEXT,                     -- ruta al PDF de la OC
    factura_documento TEXT,
    utilidad_pct      NUMERIC(6,2) DEFAULT 0,   -- margen de utilidad calculado
    fecha_registro    TIMESTAMPTZ DEFAULT NOW()
)

cotizacion_detalle (
    id              SERIAL PRIMARY KEY,
    cotizacion_id   INTEGER NOT NULL REFERENCES cotizaciones(id),
    producto_id     INTEGER NOT NULL REFERENCES productos(id),
    cantidad        NUMERIC(14,4) NOT NULL,
    precio_unitario NUMERIC(14,4) NOT NULL,
    subtotal        NUMERIC(14,4) NOT NULL,
    iva             NUMERIC(14,4) DEFAULT 0,
    total           NUMERIC(14,4) NOT NULL,
    costo_snapshot  NUMERIC(14,4),              -- costo al momento de crear la cotización
    tiene_stock     INTEGER DEFAULT 1           -- 1 = hay stock para cubrir esta línea
)

seguimiento_etapas (
    id            SERIAL PRIMARY KEY,
    cotizacion_id INTEGER NOT NULL REFERENCES cotizaciones(id),
    etapa         TEXT NOT NULL,
    completada    INTEGER DEFAULT 0,
    referencia    TEXT,
    fecha_etapa   DATE,
    notas         TEXT,
    UNIQUE(cotizacion_id, etapa)
)

entregas_parciales (
    id                 SERIAL PRIMARY KEY,
    cotizacion_id      INTEGER NOT NULL REFERENCES cotizaciones(id),
    producto_id        INTEGER NOT NULL REFERENCES productos(id),
    cantidad_entregada NUMERIC(14,4) NOT NULL,
    fecha_entrega      DATE NOT NULL,
    notas              TEXT,
    usuario            TEXT
)

documentos_cotizacion (
    id             SERIAL PRIMARY KEY,
    cotizacion_id  INTEGER NOT NULL REFERENCES cotizaciones(id),
    tipo           TEXT NOT NULL,               -- 'orden_compra', 'factura', 'remision', etc.
    nombre_archivo TEXT NOT NULL,
    ruta_archivo   TEXT NOT NULL,
    notas          TEXT
)
```

#### Compras

```sql
compras (
    id                SERIAL PRIMARY KEY,
    folio             TEXT UNIQUE NOT NULL,     -- formato: CMP-YYYY-NNNN
    proveedor_id      INTEGER REFERENCES proveedores(id),
    fecha_compra      TIMESTAMPTZ NOT NULL,
    subtotal          NUMERIC(14,4) DEFAULT 0,
    iva               NUMERIC(14,4) DEFAULT 0,
    total             NUMERIC(14,4) DEFAULT 0,
    metodo_pago_id    INTEGER REFERENCES metodos_pago(id),
    notas             TEXT,
    ticket_referencia TEXT,
    cotizacion_id     INTEGER,                  -- cotización principal vinculada (opcional)
    factura_xml_id    INTEGER,                  -- ID de la factura CFDI asociada (opcional)
    fecha_registro    TIMESTAMPTZ DEFAULT NOW()
)

compra_detalle (
    id             SERIAL PRIMARY KEY,
    compra_id      INTEGER NOT NULL REFERENCES compras(id),
    producto_id    INTEGER NOT NULL REFERENCES productos(id),
    cantidad       NUMERIC(14,4) NOT NULL,
    costo_unitario NUMERIC(14,4) NOT NULL,
    costo_total    NUMERIC(14,4) NOT NULL
)

compra_detalle_cotizacion (
    id                SERIAL PRIMARY KEY,
    compra_detalle_id INTEGER NOT NULL REFERENCES compra_detalle(id),
    cotizacion_id     INTEGER REFERENCES cotizaciones(id),
    cantidad          NUMERIC(14,4) NOT NULL,   -- cuántas unidades de esta compra se asignan a esa cotización
    notas             TEXT
)
```

#### Facturas CFDI

```sql
facturas (
    id               SERIAL PRIMARY KEY,
    uuid             TEXT UNIQUE NOT NULL,      -- UUID del timbre SAT
    serie            TEXT,
    folio_factura    TEXT,
    fecha            TEXT,
    fecha_timbrado   TEXT,
    no_cert_sat      TEXT,
    rfc_emisor       TEXT,
    nombre_emisor    TEXT,
    rfc_receptor     TEXT,
    nombre_receptor  TEXT,
    uso_cfdi         TEXT,
    tipo             TEXT,                      -- 'venta' (CLF emite) | 'compra' (CLF recibe)
    metodo_pago      TEXT,                      -- PUE, PPD
    forma_pago       TEXT,                      -- 01 efectivo, 03 transferencia, etc.
    moneda           TEXT DEFAULT 'MXN',
    subtotal         NUMERIC(14,4) DEFAULT 0,
    descuento        NUMERIC(14,4) DEFAULT 0,
    iva              NUMERIC(14,4) DEFAULT 0,
    total            NUMERIC(14,4) DEFAULT 0,
    ruta_xml         TEXT,                      -- ruta al archivo XML original
    notas            TEXT,
    fecha_registro   TIMESTAMPTZ DEFAULT NOW()
)

factura_conceptos (
    id                SERIAL PRIMARY KEY,
    factura_id        INTEGER NOT NULL REFERENCES facturas(id),
    clave_prod_serv   TEXT,                     -- clave SAT del producto/servicio
    no_identificacion TEXT,                     -- código interno del producto
    cantidad          NUMERIC(14,4),
    clave_unidad      TEXT,
    unidad            TEXT,
    descripcion       TEXT,
    valor_unitario    NUMERIC(14,4),
    importe           NUMERIC(14,4),
    descuento         NUMERIC(14,4) DEFAULT 0
)
```

#### Tablas relacionales y auxiliares

```sql
factura_cotizaciones (
    factura_id    INTEGER NOT NULL REFERENCES facturas(id),
    cotizacion_id INTEGER NOT NULL REFERENCES cotizaciones(id),
    fecha_vinculo TIMESTAMPTZ DEFAULT NOW(),
    notas         TEXT,
    UNIQUE(factura_id, cotizacion_id)
)

ordenes_compra (
    id          SERIAL PRIMARY KEY,
    numero      TEXT,
    cliente_id  INTEGER REFERENCES clientes(id),
    fecha       TEXT,
    monto_total NUMERIC(14,4) DEFAULT 0,
    documento   TEXT,                           -- ruta al archivo PDF/imagen
    notas       TEXT
)

oc_cotizaciones (
    oc_id         INTEGER NOT NULL REFERENCES ordenes_compra(id),
    cotizacion_id INTEGER NOT NULL REFERENCES cotizaciones(id),
    UNIQUE(oc_id, cotizacion_id)
)

config_permisos (
    permiso_key TEXT NOT NULL,
    rol         TEXT NOT NULL,
    PRIMARY KEY (permiso_key, rol)
)
```

---

## 4. Reglas de Negocio Críticas

### 4.1 Máquina de Estados de Cotización

Una cotización sigue esta cadena de estados (la dirección es siempre hacia adelante, salvo cancelar):

```
Pendiente → Programada → Parcialmente Entregada → Entregada → Facturada → Pagada
                                                                         ↗
              Cualquier estado (excepto Pagada) ────────────────→ Cancelada
```

| Estado | Significado operativo |
|---|---|
| **Pendiente** | Cotización enviada al cliente, esperando autorización |
| **Programada** | Cliente autorizó el pedido (orden de compra recibida) |
| **Parcialmente Entregada** | Se entregaron algunos productos, faltan otros |
| **Entregada** | Todos los productos fueron entregados al cliente |
| **Facturada** | Se emitió el CFDI de venta |
| **Pagada** | El cliente liquidó el saldo total |
| **Cancelada** | La cotización fue cancelada |

**Estados de venta confirmada** (cliente autorizó): Programada, Parcialmente Entregada, Entregada, Facturada, Pagada.

**Estados con saldo pendiente de cobro**: Programada, Parcialmente Entregada, Entregada, Facturada.

### 4.2 Generación de Folios

- **Cotización:** `COT-YYYY-NNNN` — se genera con `pg_advisory_xact_lock` para evitar duplicados en acceso concurrente.
- **Compra:** `CMP-YYYY-NNNN` — generado secuencialmente por año.
- Los folios son únicos dentro del año calendario.

### 4.3 Cálculo de Costo al Entregar

Cuando una cotización se marca como **Entregada**, el sistema calcula el costo real de cada línea con esta prioridad:

1. **Compra directa asignada** — promedio ponderado de `compra_detalle_cotizacion` para ese producto y cotización.
2. **Costo promedio del producto** — `productos.costo_promedio` si es mayor a 0.
3. **Snapshot del catálogo** — `cotizacion_detalle.costo_snapshot` (el costo al momento de crear la cotización).

Este mecanismo garantiza que el margen de utilidad calculado refleje el costo real de adquisición.

### 4.4 Clasificación ABC de Inventario

El módulo de stock clasifica automáticamente los productos por valor de inventario (`stock_actual × costo_promedio`):

| Categoría | Criterio | Significado |
|---|---|---|
| **A** | Primeros productos que acumulan el 80% del valor total | Alta importancia, control estricto |
| **B** | Siguiente tramo hasta el 95% acumulado | Importancia media |
| **C** | El restante 5% | Baja rotación o bajo valor |

### 4.5 Vinculación CFDI

El sistema detecta automáticamente **pendientes de vinculación** al importar facturas XML:

- **Clientes sin registrar** — RFC receptor de facturas de venta no existe en el catálogo.
- **Proveedores sin registrar** — RFC emisor de facturas de compra no existe en el catálogo.
- **Productos sin vincular** — `no_identificacion` o `clave_prod_serv` en conceptos no corresponde a ningún producto.
- **Facturas de venta sin cotización** — CLF emitió la factura pero no tiene cotización vinculada.
- **Facturas de compra sin registro** — CLF recibió la factura pero no hay compra registrada.

El RFC propio `CLF240418U94` se excluye del análisis de clientes y proveedores para evitar falsos positivos.

### 4.6 Movimientos de Stock

Cada cambio en inventario genera un registro en `movimientos_stock` con:
- Stock antes y después (inmutable, para auditoría)
- Tipo: `entrada` | `salida` | `ajuste`
- Referencia al folio de compra o cotización que originó el movimiento
- Usuario que realizó la acción

Los motivos de salida disponibles son: Merma/Daño, Uso interno, Devolución a proveedor, Ajuste de inventario, Muestra/Demo, Pérdida, Otro.

---

## 5. Control de Acceso (RBAC)

### Jerarquía de roles

| Rol | Nivel | Descripción |
|---|---|---|
| **Administrador** | 3 | Acceso total, incluyendo configuración del sistema |
| **Operador** | 2 | Operaciones comerciales: cotizar, comprar, facturar |
| **Almacenista** | 2 | Enfocado en stock y pre-inventario |
| **Solo lectura** | 1 | Solo consulta, sin modificaciones |

### Permisos configurables por empresa

Los permisos por defecto pueden sobreescribirse por empresa en la tabla `config_permisos`. Los permisos incluyen:

| Clave | Acción |
|---|---|
| `cotizacion.crear` | Crear nueva cotización |
| `cotizacion.editar` | Editar cotización existente |
| `cotizacion.cambiar_estado` | Avanzar el estado de una cotización |
| `cotizacion.marcar_pagada` | Solo Administrador por defecto |
| `cotizacion.ver_precios` | Ver montos e importes |
| `factura.importar` | Importar XML del SAT |
| `factura.vincular` | Vincular factura a cotización |
| `stock.entrada_manual` | Registrar entrada de inventario |
| `stock.salida_manual` | Registrar salida de inventario |
| `catalogo.crear_editar` | Crear/editar clientes, proveedores, productos |
| `catalogo.eliminar` | Eliminar registros del catálogo |
| `preinventario.capturar` | Capturar conteo físico |
| `preinventario.aprobar` | Aprobar o rechazar sesión de conteo |
| `estudio.gestionar` | Crear y capturar estudios de mercado |
| `estudio.aplicar_catalogo` | Actualizar precio de catálogo desde el estudio |

> El rol **Administrador** siempre tiene todos los permisos, sin excepción.

### Autenticación

- **Hash de contraseña:** PBKDF2-HMAC-SHA256 con 200,000 iteraciones y salt aleatorio por usuario (32 bytes hex).
- **Sesión:** JWT firmado con HS256, TTL de 8 horas.
- **Migración legacy:** El sistema detecta hashes con salt estático (esquema antiguo) y los acepta para compatibilidad durante la migración.

---

## 6. Índices de Rendimiento

```sql
-- Cotizaciones: filtrado por estado, fecha y cliente
idx_cot_estado        ON cotizaciones(estado)
idx_cot_fecha         ON cotizaciones(fecha)
idx_cot_fecha_entrega ON cotizaciones(fecha_entrega)
idx_cot_cliente       ON cotizaciones(cliente_id)

-- Facturas: búsqueda por UUID y RFC
idx_fac_uuid          ON facturas(uuid)
idx_fac_rfc_emisor    ON facturas(rfc_emisor)
idx_fac_rfc_receptor  ON facturas(rfc_receptor)

-- Productos: búsqueda por código
idx_prod_codigo       ON productos(codigo)

-- Detalle y movimientos: join por foreign key
idx_cotdet_cotizacion ON cotizacion_detalle(cotizacion_id)
idx_facdet_factura    ON factura_conceptos(factura_id)
idx_mov_producto      ON movimientos_stock(producto_id)
idx_mov_fecha         ON movimientos_stock(fecha)
```

---

## 7. Configuración del Sistema

El archivo `config.json` (no versionado) contiene:

```json
{
  "postgresql": {
    "host": "...",
    "port": 5432,
    "user": "...",
    "password": "...",
    "database_usuarios": "clf_usuarios"
  },
  "empresas": [
    {
      "nombre": "CLF",
      "pg_database": "clf_empresa",
      "rfc": "CLF240418U94"
    }
  ],
  "preferencias": {
    "empresa_key": {
      "pdf": {
        "vigencia": "30 DÍAS",
        "lugar_entrega": "MÉRIDA",
        "tiempo_entrega": "...",
        "moneda": "...",
        "cambios": "..."
      }
    }
  }
}
```

---

## 8. Generación de PDFs

El sistema genera PDFs para:

| Documento | Módulo |
|---|---|
| Cotización | `pdf_cotizacion.py` |
| Estado de cuenta por cliente | `pdf_estado_cuenta.py` |
| Presupuesto de compra | `pdf_presupuesto_compra.py` |

Los PDFs incluyen logo de la empresa, condiciones de venta (vigencia, lugar de entrega, tiempo de entrega, condiciones de precio) y se personalizan desde `PDF_CONFIG`.

---

## 9. Módulos Especiales

### Pre-inventario
Flujo de conteo físico supervisado:
1. Almacenista captura conteos por ubicación.
2. Administrador aprueba o rechaza la sesión.
3. Al aprobar, `stock_actual` de los productos se actualiza con los conteos.

### Estudio de Mercado
Permite capturar precios de mercado de proveedores para un producto y decidir si actualizar el precio base del catálogo. Requiere permiso `estudio.aplicar_catalogo` para modificar precios.

### Estado de Cuenta
Vista consolidada de todas las cotizaciones de un cliente con saldo pendiente, mostrando montos entregados, facturados y pagados, y la diferencia por cobrar.

### Análisis de Costos
Módulo de reportes con métricas de utilidad, rotación de inventario y comparativo de ventas por período.

---

## 10. Consideraciones para Desarrollo Futuro

- **Multi-empresa:** La arquitectura ya soporta múltiples bases de datos. Para agregar una empresa nueva se necesita crear la base de datos PostgreSQL, ejecutar `migrate_schema.py` y registrar la empresa en `config.json`.
- **Costo promedio ponderado:** El campo `productos.costo_promedio` se actualiza al registrar compras. La lógica de actualización debe garantizar atomicidad con el movimiento de stock.
- **Auditoría:** Existe un módulo `audit.py` que registra acciones sensibles. Las tablas de auditoría no están en este esquema porque se gestionan por separado.
- **Seguridad del JWT:** El `SECRET_KEY` para firmar tokens se configura en variables de entorno o en `web_app/config.py`. No debe estar hardcodeado en código.
- **Backups:** La base `clf_usuarios` es crítica; su pérdida bloquea el acceso a todas las empresas. Debe tener respaldo independiente.



---
title: User Journey & UX Observations - CLF Gestión
date: 2024-05-05
tags:
  - user-journey
  - ux-design
  - b2b
  - clf-gestion
status: analysis
---

# 🚀 CLF Gestión: User Journey & Observations

Este documento analiza el recorrido del usuario dentro del ecosistema de **CLF Gestión** y propone mejoras estratégicas para optimizar la experiencia operativa y administrativa.

---

## 🗺️ 1. User Journey Map (Flujo B2B)

El sistema CLF no es una simple tienda; es un orquestador de ciclos comerciales complejos. A continuación se detalla el viaje desde el prospecto hasta el cobro.

```mermaid
graph TD
    subgraph "Fase 1: Comercial"
        A[Inicio: Dashboard] --> B[Nueva Cotización]
        B --> C{Autorización Cliente}
        C -->|No| D[Seguimiento/Ajuste]
        C -->|Sí| E[Estado: Programada]
    end

    subgraph "Fase 2: Operativa"
        E --> F[Compra a Proveedor]
        F --> G[Recepción de Stock]
        G --> H[Entrega al Cliente]
        H --> I{¿Entrega Parcial?}
        I -->|Sí| H
        I -->|No| J[Estado: Entregada]
    end

    subgraph "Fase 3: Administrativa"
        J --> K[Vinculación CFDI]
        K --> L[Facturación de Venta]
        L --> M[Estado de Cuenta]
        M --> N[Registro de Pago]
        N --> O[Estado: Pagada]
    end

    class A,B,E,J,O success;
    class K,L,M info;
```

---

## 🔍 2. Etapas y Puntos de Contacto

### 🟢 Etapa 1: Captura y Negociación
**Acciones:** Crear clientes, buscar productos, generar PDF de cotización.
**Puntos Fuertes:** 
- Generación automatizada de folios `COT-YYYY-NNNN`.
- Snapshots de costo para proteger el margen.

> [!tip] Observación de Mejora
> Implementar un **"Quick-Add"** de productos desde la misma pantalla de cotización para evitar salir al catálogo si un ítem es nuevo o tiene un precio especial de una sola vez.

### 🟡 Etapa 2: Ejecución y Logística
**Acciones:** Registrar compras, movimientos de stock, entregas parciales.
**Fricción detectada:** 
- Las **Entregas Parciales** pueden ser confusas de rastrear visualmente si no hay una barra de progreso clara por cada concepto de la cotización.

> [!warning] Riesgo Operativo
> Si el almacenista no registra la entrega a tiempo, el cálculo de **utilidad real** se retrasa, afectando los reportes financieros en tiempo real.

### 🔵 Etapa 3: Conciliación Fiscal (CFDI)
**Acciones:** Importación de XML, vinculación con cotizaciones/compras.
**Observación:** 
- El sistema ya detecta RFCs y productos no registrados. Esta es la funcionalidad más potente pero también la más densa.

---

## 💡 3. Observaciones Estratégicas para una Mejor Experiencia

### 🎨 Estética y Visualización
*   **Dashboards Predictivos:** Actualmente el dashboard muestra el estado actual. Sería ideal incluir una sección de **"Próximos a Vencer"** (cotizaciones sin OC, facturas por cobrar).
*   **Micro-interacciones:** Usar animaciones sutiles (vía HTMX o React) al cambiar estados de cotización para dar feedback visual inmediato de que el proceso "avanza" en la cadena de suministro.

### ⚙️ Automatización Operativa
*   **Matching Inteligente:** Usar lógica Difusa (Fuzzy Match) para sugerir vinculaciones entre conceptos de factura XML y productos del catálogo cuando el nombre no es idéntico.
*   **Alertas de Margen:** Resaltar en ==Rojo== si el costo de compra capturado es superior al `costo_snapshot`, alertando antes de que la utilidad se vuelva negativa.

---

## 🛠️ 4. Propuesta de Valor UI/UX

> [!abstract] Resumen de Recomendaciones
> 1. **Visualizador de Flujo:** Agregar una línea de tiempo (Stepper) en la vista de detalle de cada cotización que muestre en qué punto del ciclo se encuentra (Pendiente -> Programada -> ...).
> 2. **Mobile-First para Almacén:** Optimizar las vistas de `preinventario` y `stock` para dispositivos móviles (usando Tailwind) para que el almacenista capture desde el pasillo.
> 3. **Central de Documentos:** Unificar la visualización de PDF de cotización, XML de factura y OC en un solo "Expediente Digital" por cada folio.

---

## 📈 Conclusión

El sistema **CLF Gestión** tiene una base técnica robusta y una lógica de negocio sofisticada (especialmente en el cálculo de costos y vinculación CFDI). El siguiente paso para lograr un "User Journey" premium es la **visibilidad proactiva**: que el usuario no tenga que buscar el dato, sino que el sistema le alerte sobre lo que requiere su atención inmediata.

---
*Documento generado para el equipo de desarrollo de CLF.*



---
title: CLF — Servidor, Controladores y Seguridad
tags:
  - clf-gestion
  - backend
  - seguridad
  - arquitectura
aliases:
  - CLF Backend
  - CLF Servidor
date: 2026-05-06
status: vigente
---

# CLF — Servidor, Controladores y Seguridad

> Referencia técnica del servidor FastAPI de CLF Gestion. Cubre la arquitectura del backend, cada controlador (router), el sistema de autenticación, autorización por roles y permisos, auditoría y monitoreo.
> Complementa → [[Gestión CLF]]

---

## 1. Mapa del Servidor

El servidor es una aplicación **FastAPI** (Python) que expone dos tipos de interfaz en paralelo:

| Tipo | Tecnología | Para quién |
|---|---|---|
| **HTML con plantillas** | Jinja2 + HTMX | Interfaz web tradicional (navegador directo) |
| **API JSON** | FastAPI + JSON | React SPA (Single Page App) en `frontend/` |

Ambas comparten la misma lógica de negocio y base de datos. La diferencia es solo la presentación de la respuesta.

```mermaid
graph TD
    Browser["🌐 Navegador / React SPA"]
    Browser -->|Cookie JWT| HTML["Routers HTML\nJinja2 + HTMX"]
    Browser -->|Bearer Token| API["Routers API\nJSON puro"]

    HTML --> BL["Lógica de Negocio\n(queries PostgreSQL)"]
    API --> BL

    BL --> DB1["clf_usuarios\n(usuarios globales)"]
    BL --> DB2["clf_empresa\n(datos operativos)"]
    BL --> DB3["clf_pte\n(otra empresa)"]

    subgraph Seguridad
        JWT["JWT HS256\n8 horas"]
        RBAC["RBAC\nRoles + Permisos"]
        Audit["Audit Log"]
    end

    HTML --> Seguridad
    API --> Seguridad
```

### Archivos principales del servidor

| Archivo | Propósito |
|---|---|
| `web_app/main.py` | Punto de entrada, middleware, registro de routers, arranque |
| `web_app/auth.py` | Hash de contraseñas + JWT |
| `web_app/rbac.py` | Roles y permisos configurables |
| `web_app/dependencies.py` | Extracción del usuario del token (inyección de dependencias) |
| `web_app/database.py` | Pool de conexiones PostgreSQL |
| `web_app/config.py` | Configuración por entorno (dev/prod) |
| `web_app/audit.py` | Log de eventos de seguridad |
| `web_app/cache.py` | Caché en memoria con TTL |
| `web_app/metrics.py` | Métricas de peticiones en tiempo real |
| `web_app/logger.py` | Logging estructurado con rotación diaria |

---

## 2. Arranque y Middleware

### Secuencia de arranque (`lifespan`)

```mermaid
sequenceDiagram
    participant Server
    participant Logging
    participant AuditDB
    participant Pools
    participant Migrations

    Server->>Logging: Inicializar logger (rotación diaria)
    Server->>AuditDB: Crear tabla audit_log si no existe
    Server->>Pools: Crear pool clf_usuarios (2–5 conex)
    loop Por cada empresa en config.json
        Server->>Pools: Crear pool empresa (2–10 conex)
        Server->>Migrations: ALTER TABLE / CREATE TABLE IF NOT EXISTS
    end
    Server-->>Server: ✅ Listo para peticiones
```

> [!info] Migraciones en caliente
> Cada arranque ejecuta `ALTER TABLE` e `CREATE TABLE IF NOT EXISTS` para agregar columnas o tablas nuevas sin romper datos existentes. Los errores de migración se loguean como advertencias y no detienen el arranque.

### Stack de middleware (en orden de ejecución)

```
Petición entrante
       ↓
1. MonitorMiddleware    → mide tiempo, agrega headers de seguridad, loguea queries lentas (>2s)
       ↓
2. CORSMiddleware       → valida origen de la petición (origins permitidos hardcodeados)
       ↓
3. RateLimiter          → máximo 10 req/min en /auth/login
       ↓
   Router correspondiente
```

### Headers de seguridad (agregados por `MonitorMiddleware`)

```
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: geolocation=(), microphone=(), camera=(self)
```

### CORS — Origins permitidos

| Origin | Uso |
|---|---|
| `http://localhost:5173` | Vite dev server |
| `http://localhost:4173` | Vite preview |
| `https://clf-gestion.web.app` | Firebase Hosting (producción) |
| `https://clf-gestion.firebaseapp.com` | Firebase dominio alternativo |

---

## 3. Sistema de Autenticación

### 3.1 Contraseñas

El hash se genera con **PBKDF2-HMAC-SHA256**, 200,000 iteraciones, con un salt aleatorio único por usuario.

```python
# Resultado: (hash_hex, salt_hex)
hash_password("mi_contraseña") → ("abc123...", "salt456...")
```

> [!warning] Migración de esquema legacy
> Existe un esquema antiguo con salt estático (hardcodeado). Si el sistema detecta un hash antiguo al hacer login, lo acepta para permitir el acceso pero **reemplaza automáticamente el hash** por uno nuevo con salt aleatorio. El usuario nunca lo nota.

### 3.2 Token JWT

```mermaid
sequenceDiagram
    participant User
    participant Server
    participant DB as clf_usuarios

    User->>Server: POST /auth/login (usuario, password, empresa_id)
    Server->>DB: SELECT hash, salt FROM usuarios WHERE username=?
    DB-->>Server: hash + salt
    Server->>Server: verificar_password() → OK
    Server->>Server: crear_token(payload) → JWT HS256
    Server->>Server: registrar audit: LOGIN_OK
    Server-->>User: Set-Cookie: access_token=JWT (httponly)
```

**Contenido del payload del JWT:**

```json
{
  "sub": 42,
  "username": "oscar",
  "nombre": "Oscar Gómez",
  "rol": "Administrador",
  "empresa_id": "emp_CLF240418U94_default",
  "empresa_db": "clf_empresa",
  "empresa_nombre": "COMERCIALIZADORA...",
  "exp": 1234567890
}
```

> [!tip] Dos modos de autenticación
> - **Interfaz HTML** → Cookie `access_token` (httponly, SameSite=Lax). El navegador la envía automáticamente.
> - **API JSON / React SPA** → Header `Authorization: Bearer <token>`. El token se guarda en `sessionStorage` del navegador.

### 3.3 Extracción del usuario (dependencias FastAPI)

| Función | Archivo | Usa | Falla si |
|---|---|---|---|
| `get_usuario_actual(request)` | `dependencies.py` | Cookie | — (retorna `None` si no hay token) |
| `get_usuario_api(Authorization)` | `dependencies.py` | Header Bearer | Lanza HTTP 401 |

---

## 4. RBAC — Roles y Permisos

Ver también → [[Gestión CLF#5. Control de Acceso (RBAC)]]

### 4.1 Roles y niveles

| Rol | Nivel numérico | Descripción |
|---|---|---|
| **Administrador** | 3 | Acceso total, no se puede restringir |
| **Operador** | 2 | Ventas, compras, facturas |
| **Almacenista** | 2 | Stock, preinventario |
| **Solo lectura** | 1 | Solo consulta |

### 4.2 Permisos configurables por empresa

Los permisos se pueden personalizar por empresa en la tabla `config_permisos`. Si no hay configuración custom, se usan los defaults:

| Permiso | Admin | Operador | Almacenista | Solo lectura |
|---|:---:|:---:|:---:|:---:|
| `cotizacion.crear` | ✅ | ✅ | ❌ | ❌ |
| `cotizacion.editar` | ✅ | ✅ | ❌ | ❌ |
| `cotizacion.cambiar_estado` | ✅ | ✅ | ❌ | ❌ |
| `cotizacion.marcar_pagada` | ✅ | ❌ | ❌ | ❌ |
| `cotizacion.ver_precios` | ✅ | ✅ | ✅ | ❌ |
| `factura.importar` | ✅ | ✅ | ❌ | ❌ |
| `factura.vincular` | ✅ | ✅ | ❌ | ❌ |
| `factura.ver_montos` | ✅ | ✅ | ✅ | ❌ |
| `stock.entrada_manual` | ✅ | ❌ | ❌ | ❌ |
| `stock.salida_manual` | ✅ | ❌ | ❌ | ❌ |
| `catalogo.crear_editar` | ✅ | ✅ | ❌ | ❌ |
| `catalogo.eliminar` | ✅ | ❌ | ❌ | ❌ |
| `preinventario.capturar` | ✅ | ✅ | ✅ | ❌ |
| `preinventario.aprobar` | ✅ | ❌ | ❌ | ❌ |
| `estudio.gestionar` | ✅ | ✅ | ❌ | ❌ |
| `estudio.aplicar_catalogo` | ✅ | ❌ | ❌ | ❌ |

> [!important] Administrador siempre tiene acceso
> El rol Administrador ignora la tabla `config_permisos`. Sus permisos no se pueden revocar por configuración.

### 4.3 Cómo se protegen las rutas

```python
# Requiere un rol específico
@router.post("/usuarios/nuevo")
async def crear_usuario(user=Depends(require_rol("Administrador"))):
    ...

# Requiere un permiso configurable
@router.post("/cotizaciones")
async def crear_cotizacion(user=Depends(require_permiso("cotizacion.crear"))):
    ...
```

**Caché de permisos:** Los permisos se cachean en memoria por 5 minutos por empresa. Se invalida automáticamente cuando el admin guarda cambios en `/admin/permisos`.

---

## 5. Controladores (Routers)

### 5.1 Autenticación — `routers/auth.py`

| Endpoint | Método | Descripción |
|---|---|---|
| `/auth/login` | POST | Login HTML con cookie. Rate limit: 10/min |
| `/auth/logout` | GET | Borra la cookie y redirige a `/` |
| `/api/auth/login` | POST | Login JSON para React SPA. Retorna Bearer token |
| `/api/auth/logout` | GET | No-op (token se borra en el cliente) |
| `/api/auth/empresas` | GET | Lista de empresas disponibles |

**Flujo de login detallado:**
1. Valida que `empresa_id` exista en `config.json`
2. Busca usuario en `clf_usuarios` por username
3. Verifica password (nuevo o legacy)
4. Si legacy → reemplaza hash al nuevo esquema
5. Actualiza `ultimo_acceso` del usuario
6. Genera JWT e inyecta en cookie/respuesta
7. Registra en `audit_log`: `LOGIN_OK` o `LOGIN_FALLO`

### 5.2 Administración — `routers/admin.py`

> [!note] Todos los endpoints de `/admin/*` requieren rol Administrador

**Gestión de usuarios:**

| Endpoint | Método | Descripción |
|---|---|---|
| `/admin/usuarios` | GET | Lista todos los usuarios con último acceso |
| `/admin/usuarios/nuevo` | GET | Formulario de creación |
| `/admin/usuarios/nuevo` | POST | Crear usuario (mínimo 6 chars de password) |
| `/admin/usuarios/{id}/editar` | GET/POST | Ver y guardar cambios de usuario |
| `/admin/usuarios/{id}/toggle` | POST | Activar/desactivar (HTMX, retorna fragmento HTML) |

> [!warning] Restricción de auto-desactivación
> Un usuario no puede desactivarse a sí mismo. Si el admin intenta hacer toggle de su propio usuario, el servidor retorna un error.

**Gestión de permisos:**

| Endpoint | Método | Descripción |
|---|---|---|
| `/admin/permisos` | GET | Matriz de permisos actual vs defaults |
| `/admin/permisos` | POST | Guardar overrides en `config_permisos` + invalida caché |

**Gestión de ubicaciones (sucursales y áreas):**

| Endpoint | Método | Descripción |
|---|---|---|
| `/admin/ubicaciones` | GET | Lista sucursales + áreas |
| `/admin/sucursales` | POST | Crear sucursal |
| `/admin/sucursales/{id}/editar` | POST | Editar sucursal |
| `/admin/sucursales/{id}/eliminar` | POST | Eliminar (falla si tiene áreas con stock) |
| `/admin/areas` | POST | Crear área |
| `/admin/areas/{id}/editar` | POST | Editar área |
| `/admin/areas/{id}/eliminar` | POST | Eliminar (falla si tiene productos asignados) |

### 5.3 Dashboard — `routers/api_dashboard.py`

**`GET /api/dashboard`** — Requiere usuario autenticado.

Retorna un JSON con toda la información del panel principal:

```
kpis
├── monto_vendido        → suma de cotizaciones en estados de venta confirmada (mes actual)
├── pendiente_cobrar     → suma de cotizaciones Programada/Entregada/Facturada
├── num_pendientes       → conteo de cotizaciones en estado Pendiente
└── num_programadas      → conteo de cotizaciones Programadas

bloques
├── comercial
│   ├── sin_respuesta    → cotizaciones Pendiente con >15 días sin respuesta del cliente
│   └── sin_oc           → cotizaciones Programadas sin orden de compra del cliente registrada
├── logistico
│   ├── sin_stock        → cotizaciones con líneas sin stock suficiente
│   └── entregas_pendientes → cotizaciones Programadas listas para entregar
└── administrativo
    └── facturas_sin_pago → cotizaciones Entregadas/Facturadas con factura pendiente de pago

embudo                   → conteo y monto por cada estado de cotización
stock_bajo               → productos con stock_actual < stock_minimo
rol                      → rol del usuario que consulta (para adaptar la UI)
```

### 5.4 Cotizaciones — `routers/api_cotizaciones.py`

| Endpoint | Método | Descripción |
|---|---|---|
| `/api/cotizaciones` | GET | Lista paginada (25/página). Filtros: estado, búsqueda |
| `/api/cotizaciones/{id}` | GET | Detalle completo: líneas + etapas + facturas + compras vinculadas |
| `/api/cotizaciones` | POST | Crear cotización |
| `/api/cotizaciones/buscar-producto` | GET | Autocompletado de productos con stock y precio |

> [!tip] Folio sin duplicados
> La creación usa `pg_advisory_xact_lock()` de PostgreSQL para serializar la asignación del folio `COT-YYYY-NNNN` y garantizar que dos usuarios simultáneos no obtengan el mismo número.

**Modelo de creación (`CotizacionIn`):**
```
cliente_id, comprador_id, fecha, orden_compra, notas, utilidad_pct
partidas[]
  └── producto_id / descripcion_libre, cantidad, precio_unitario, aplica_iva
```

Al crear, el sistema guarda `costo_snapshot` en cada línea (el costo del producto en ese momento) para que el margen calculado después sea comparable.

### 5.5 Compras — `routers/api_compras.py`

| Endpoint | Método | Descripción |
|---|---|---|
| `/api/compras` | GET | Lista paginada de órdenes de compra |
| `/api/compras/{id}` | GET | Detalle: líneas + cotizaciones vinculadas por línea + factura XML |

### 5.6 Stock — `routers/api_stock.py`

| Endpoint | Método | Descripción |
|---|---|---|
| `/api/stock` | GET | Lista de productos con stock. Filtros: búsqueda, categoría, solo_bajo_minimo |
| `/api/stock/movimientos` | GET | Historial paginado de movimientos. Filtro: tipo (entrada/salida/ajuste) |

**Estadísticas que retorna `/api/stock`:**
- `total_valor` — valor total del inventario (stock × costo promedio)
- `bajo_minimo` — conteo de productos por debajo del mínimo
- `negativo` — conteo de productos con stock negativo (error de datos)

### 5.7 Catálogos — `routers/api_catalogos.py`

| Endpoint | Método | Descripción |
|---|---|---|
| `/api/catalogos/clientes` | GET | Lista de clientes activos con métricas (# cotizaciones, última fecha, monto total) |
| `/api/catalogos/clientes/{id}` | GET | Detalle del cliente + últimas cotizaciones |
| `/api/catalogos/productos` | GET | Lista de productos |
| `/api/catalogos/proveedores` | GET | Lista de proveedores |

### 5.8 Facturas CFDI — `routers/api_facturas.py`

| Endpoint | Método | Descripción |
|---|---|---|
| `/api/facturas` | GET | Lista paginada (30/página). Filtros: búsqueda, tipo (venta/compra) |
| `/api/facturas/{id}` | GET | Detalle: conceptos + cotizaciones vinculadas + compras |
| `/api/facturas` | POST | Subir XML CFDI → parsear y almacenar |

### 5.9 Rutas de monitoreo y utilidades

| Endpoint | Auth | Descripción |
|---|---|---|
| `GET /health` | No | Health check básico |
| `GET /api/health` | No | Health check para monitoreo externo |
| `GET /admin/metrics` | Admin | Snapshot de métricas de peticiones |
| `GET /admin/audit` | Admin | Visor del log de auditoría |
| `GET /manifest.json` | No | Manifest PWA |
| `GET /sw.js` | No | Service worker (caché offline) |

---

## 6. Auditoría de Seguridad

**Archivo:** `web_app/audit.py`  
**Tabla:** `audit_log` en `clf_usuarios`

### Eventos que se registran

| Evento | Cuándo |
|---|---|
| `LOGIN_OK` | Login exitoso |
| `LOGIN_FALLO` | Password incorrecto o usuario inexistente |
| `LOGOUT` | Cierre de sesión |
| `ACCESO_DENEGADO` | HTTP 403 en cualquier ruta |
| `USUARIO_CREADO` | Nuevo usuario creado por admin |
| `USUARIO_EDITADO` | Datos de usuario modificados |
| `USUARIO_ACTIVADO` | Usuario reactivado |
| `USUARIO_DESACTIVADO` | Usuario desactivado |
| `PERMISOS_ACTUALIZADOS` | Admin guardó nueva configuración de permisos |

### Estructura del registro

```sql
audit_log (
    id          SERIAL PRIMARY KEY,
    usuario_id  INTEGER,        -- puede ser NULL si el login falló
    username    TEXT,
    evento      TEXT,           -- uno de los eventos de arriba
    detalle     TEXT,           -- descripción libre del contexto
    ip          TEXT,           -- IP del cliente
    ts          TIMESTAMPTZ DEFAULT NOW()
)
```

> [!success] Falla silenciosa
> Si el sistema de auditoría falla (por ejemplo, la base de datos no responde), el error se loguea internamente pero **no interrumpe la petición del usuario**. La auditoría es best-effort.

---

## 7. Base de Datos — Pool de Conexiones

**Archivo:** `web_app/database.py`

### Arquitectura de pools

```
clf_usuarios         → ThreadedConnectionPool (2 – 5 conexiones)
clf_empresa          → ThreadedConnectionPool (2 – 10 conexiones)  ← pool por empresa
clf_pte              → ThreadedConnectionPool (2 – 10 conexiones)
```

Cada empresa tiene su propio pool, creado la primera vez que se necesita y mantenido hasta que el servidor se apaga.

### Compatibilidad SQLite → PostgreSQL

El código hereda consultas del sistema de escritorio original (SQLite). Para no reescribir todo, `database.py` incluye una capa de traducción automática:

| SQLite | PostgreSQL |
|---|---|
| `?` (placeholder) | `%s` |
| `GROUP_CONCAT()` | `STRING_AGG()` |
| `strftime()` | `TO_CHAR()` |
| `INSERT OR IGNORE` | `ON CONFLICT DO NOTHING` |
| `lastrowid` | `RETURNING id` (agregado automáticamente) |

> [!info] Configuración de conexión — orden de prioridad
> 1. Variables de entorno (`CLF_PG_HOST`, `CLF_PG_USER`, `CLF_PG_PASSWORD`, etc.)
> 2. `config.json` local
> 3. Defaults: `localhost:5432`, usuario `postgres`
>
> Para **Cloud Run** (Google): usar `CLF_CLOUD_SQL_INSTANCE=proyecto:region:instancia` → conecta vía Unix socket.

---

## 8. Caché en Memoria

**Archivo:** `web_app/cache.py`  
Thread-safe, TTL configurable por clave, sin dependencia de Redis.

| Clave | TTL | Contenido |
|---|---|---|
| `permisos:{empresa_db}` | 5 min | Permisos configurados por empresa |
| `kpis:{empresa_db}` | 2 min | Datos del dashboard |
| `catalogos:{empresa_db}` | 5 min | Catálogos de clientes/productos |
| `categorias:{empresa_db}` | 10 min | Categorías de productos |

```python
# Uso típico
cache.set("permisos:clf_empresa", dict_permisos, ttl=300)
datos = cache.get("permisos:clf_empresa")   # None si expiró
cache.invalidar("permisos:")                # Borra todo lo que empiece con "permisos:"
```

---

## 9. Configuración por Entorno

**Archivo:** `web_app/config.py`

| Variable de entorno | Default | Descripción |
|---|---|---|
| `CLF_ENV` | `development` | Entorno activo |
| `CLF_SECRET_KEY` | Auto-generado | Clave de firma JWT. **Obligatoria en producción** |
| `CLF_HOST` | `0.0.0.0` | Host del servidor |
| `CLF_PORT` | `8000` | Puerto HTTP |
| `CLF_HTTPS_PORT` | `8443` | Puerto HTTPS |
| `CLF_PG_HOST` | Desde config.json | Host PostgreSQL |
| `CLF_PG_USER` | `postgres` | Usuario PostgreSQL |
| `CLF_PG_PASSWORD` | Desde config.json | Password PostgreSQL |
| `CLF_CLOUD_SQL_INSTANCE` | — | Para Cloud Run: `proj:region:inst` |
| `CLF_POOL_MIN` / `CLF_POOL_MAX` | `2` / `10` | Tamaño del pool por empresa |

> [!danger] Producción sin SECRET_KEY = crash al arrancar
> Si `CLF_ENV=production` y `CLF_SECRET_KEY` no está definido, el servidor lanza `RuntimeError` y no arranca. En desarrollo, se genera una clave aleatoria por sesión (los tokens no persisten entre reinicios).

---

## 10. Frontend React (SPA)

**Directorio:** `frontend/`  
**Stack:** React 19 + Vite 8 + React Router 7

### Autenticación en el cliente

```
login → POST /api/auth/login → token en sessionStorage["clf_token"]
                                         ↓
                               apiClient agrega header: Authorization: Bearer {token}
                                         ↓
                               401 recibido → borrar sessionStorage + redirigir a /login
```

### Proxy de desarrollo

En desarrollo, Vite redirige `/api/*` → `http://localhost:8000` automáticamente. En producción, la SPA se despliega en Firebase Hosting y hace fetch directo al servidor.

---

## 11. Manejadores de Error Globales

Definidos en `main.py`:

| Código | Acción |
|---|---|
| **401** | Redirige a `/auth/login` (HTML) o retorna JSON error (API) |
| **403** | Renderiza página "acceso denegado" + registra en `audit_log` |
| **404** | Renderiza página "no encontrado" |
| **500** | Loguea traceback completo + renderiza página de error genérica |

---

## 12. Despliegue

### Local (desarrollo)

```bash
# Backend
uvicorn web_app.main:app --reload --host 0.0.0.0 --port 8000

# Frontend (otra terminal)
cd frontend && npm run dev
```

### Producción (Google Cloud Run)

Variables de entorno requeridas:

```
CLF_ENV=production
CLF_SECRET_KEY=<clave aleatoria fuerte, mínimo 32 chars>
CLF_CLOUD_SQL_INSTANCE=proyecto:region:instancia
CLF_PG_USER=clf_app
CLF_PG_PASSWORD=<password>
CLF_PG_DB_USUARIOS=clf_usuarios
CLF_EMPRESAS_JSON=[{"id":"...","nombre":"...","pg_database":"clf_empresa"}]
```

Frontend: build con `npm run build` → deploy a Firebase Hosting.

---

## 13. Resumen de Seguridad

```mermaid
graph LR
    A["Usuario intenta acceder"] --> B{¿Tiene token válido?}
    B -->|No| C["HTTP 401 → Redirige a login"]
    B -->|Sí| D{¿Tiene el rol requerido?}
    D -->|No| E["HTTP 403 → Audit log"]
    D -->|Sí| F{¿Tiene el permiso requerido?}
    F -->|No| E
    F -->|Sí| G["✅ Accede al recurso"]
```

| Capa | Mecanismo | Dónde |
|---|---|---|
| **Transporte** | HTTPS con certificado SSL | nginx / Cloud Run |
| **Autenticación** | JWT HS256, 8h, cookie httponly | `auth.py` |
| **Rate limiting** | 10 req/min en login | `main.py` middleware |
| **Roles** | 4 roles con jerarquía numérica | `rbac.py` |
| **Permisos** | 16 permisos configurables por empresa | `rbac.py` + `config_permisos` |
| **Auditoría** | Log de todos los eventos sensibles | `audit.py` |
| **Headers** | Anti-clickjacking, nosniff, XSS | `MonitorMiddleware` |
| **CORS** | Origins hardcodeados, credentials=true | `main.py` |
| **Multi-tenant** | Base de datos separada por empresa | `database.py` |
| **Contraseñas** | PBKDF2-SHA256, 200k iter, salt por usuario | `auth.py` |

---

%%
Generado: 2026-05-06
Fuente: revisión directa del código en CLF_gestion/
Siguiente revisión sugerida: cuando se agreguen nuevos routers o se modifique auth.py / rbac.py
%%



